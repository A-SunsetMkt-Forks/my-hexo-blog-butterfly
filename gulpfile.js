import gulp from 'gulp';
import imagemin from 'gulp-imagemin';

export default () => (
	/**
	gulp.src('src/images/*')
		.pipe(imagemin())
		.pipe(gulp.dest('dist/images'))
	*/
	/* Do nothing now */
	console.log('gulpfile.js is unused now, this is just a placeholder')
);